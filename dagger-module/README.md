# dagger-module

A Dagger module for maintaining other Dagger modules as a toolchain.

## use

Install and generate:

```sh
dagger toolchain install github.com/frantjc/daggerverse/dagger-module
dagger generate
```

Local dependencies are recursed into.
